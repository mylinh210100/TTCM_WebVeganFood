@model IEnumerable<DAO.Model.ComboFoodDetail>
@{
    ViewBag.Title = "ComboFood";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<link rel="stylesheet" href="~/Content/css/admin.css" />

<div class="container-fluid">
    @section titlepage{
        Combo food detail Page
    }
    <div class="mt-2 mb-3">
        @Html.ActionLink("Insert new detail", "InsertComboFood", "ComboDetail")
    </div>
    <div class="row">
        <table class="table table-bordered">
            <thead class="text-center">
                <tr>
                    <th>Id Combo</th>
                    <th>Id Food</th>
                    <th>Price ($)</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.IdCombo</td>
                        <td>@item.IdFood</td>
                        <td>@item.Price</td>
                        <td><a href="~/Admin/ComboDetail/DeleteComboFood/@item.IdFood">Delete</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


